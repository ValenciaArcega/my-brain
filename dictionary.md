# Here we are!
Collection of all my knowledges in <font color="#1c7ed6">one file</font>!

## <font color="#74c0fc"> Babel </font>
Es un transpilador (no compilador) de código JavaScript que permite utilizar funciones modernas y novedades del lenguaje sin necesidad de esperar a que los motores de los navegadores decidan implementarlas.
```
Babel Input: ES2015 arrow function
［1, 2, 3］.map（n → n + 1)；

Babel Output: ES5 equivalent
 [1, 2, 3].map(function(n) {
  return n + 1;
｝）；
```
## <font color="#74c0fc"> WebPack </font>
Web pack nos permitirá empaquetar las dependencias y generar archivos estáticos con el código necesario